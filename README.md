# Multi Hotels Management System

A web application to manage **multiple hotels** from one platform — rooms, reservations, guests, and staff workflows — built with **Laravel** and a modern **React (TypeScript) front-end**.

> Repo: `YounesWebDev/Multi-hotels-management-system`

---

## ✨ Features (high level)

- ✅ Multi-hotel management (manage more than one hotel in the same app)
- ✅ Authentication & authorization (roles/permissions ready)
- ✅ Rooms management (types, prices, availability)
- ✅ Reservations / bookings management
- ✅ Guest/customer management
- ✅ Admin dashboard (statistics & quick actions)
- ✅ Responsive UI

> Note: Adapt this list to match exactly what your current modules include.

---

## 🧰 Tech Stack

- **Backend:** Laravel (PHP)
- **Frontend:** React + TypeScript (Vite)
- **Database:** MySQL (recommended)
- **DevOps:** Docker + GitHub Actions workflows

(Your repo is mostly PHP + TypeScript, matching a Laravel + React setup.)  

---

## 📦 Project Structure

Common folders you’ll see:

- `app/` – Laravel application logic
- `routes/` – Web routes
- `resources/` – Front-end assets (React/TS)
- `database/` – Migrations/seeders/factories
- `public/` – Public entry
- `docker/` + `Dockerfile` – Containerization setup
- `.github/workflows/` – CI pipelines

---

## ✅ Requirements

Make sure you have:

- PHP **8.2+**
- Composer
- Node.js **18+** (recommended)
- MySQL (or compatible DB)
- (Optional) Docker

---

## 🚀 Local Setup (without Docker)

### 1) Clone the project
```bash
git clone https://github.com/YounesWebDev/Multi-hotels-management-system.git
cd Multi-hotels-management-system
